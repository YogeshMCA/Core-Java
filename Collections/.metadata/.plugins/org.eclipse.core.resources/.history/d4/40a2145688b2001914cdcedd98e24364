package com.example.java8;

import java.util.Arrays;
import java.util.Comparator;

public class JavaFeatures {

	public static void main(String[] args) {
		Comparator<String> comparator = new Comparator<String>(){

			@Override
			public int compare(String arg0, String arg1) {
				return Integer.compare(arg0.length(), arg1.length());
			}
			
		};
		
		Lambda lam = (String s)->System.out.println(s);
		lam.disp("Hello");
		Comparator<String> compLam = (s1,s2)-> Integer.compare(s1.length(), s2.length());
		
		String str[] = {"Naveen","ArunKumar","Jaga"};
		Arrays.sort(str,comparator); //Sorting Algorithem - Quick Sort
		for(String s: str)
			System.out.println(s);
	}

}

interface Lambda{
	void disp(String s);
	void display(String s,String s1);
}